# 安全要求

> 本文档描述 CodeMind 的安全规范，所有涉及认证、加密、审计的代码必须遵守。

## 密码安全

- **哈希算法**：bcrypt
- **Cost Factor**：12
- **传输**：前端通过 HTTPS 传输，后端不记录明文密码

## API Key 安全

| 属性 | 规范 |
|------|------|
| 格式 | `cm-{32-char-hex}` |
| 存储 | SHA-256 哈希，不存储明文 |
| 展示 | 仅创建时返回完整密钥，之后仅展示前缀 |
| 日志 | 禁止在日志中记录完整 API Key |
| 缓存 | Redis 缓存哈希后的 Key 信息（300s TTL） |

## 第三方 API Key 安全

- **加密算法**：AES（`internal/pkg/crypto/aes.go`）
- **存储**：第三方服务商的 API Key 加密后存储于数据库
- **解密**：仅在代理请求时解密，不在日志或 API 响应中暴露

## JWT 安全

| 属性 | 规范 |
|------|------|
| 算法 | HS256 |
| 有效期 | 24 小时 |
| 黑名单 | 登出或密码修改时加入 Redis 黑名单 |
| 传递 | `Authorization: Bearer <token>` |

## 审计日志

所有敏感操作必须记录审计日志，包含：
- 操作者信息
- 操作者 IP 地址
- 操作时间戳
- 操作详情

## 相关文档

- [核心设计模式](design-patterns.md) — 登录锁定、软删除
- [配置管理](configuration.md) — Redis Key 模式、JWT 配置
- [API 路由与认证](api-routes.md) — 认证方式与路由权限
